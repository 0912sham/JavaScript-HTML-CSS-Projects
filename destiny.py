class Bank():

	def__init__(self,owner,amount)
		self.owner = owner
		self.amount = amount

	def deposit(self,cash)
		self.amount = self.amount + cash
		print('here is your new balance')

	def withdrawal(self,money)
		if money> self.amount:
			print('insufficient amount')
			 else:
			 self.amount = self.amount + money
			 print('here is ur new balance')

	jack = Bank(ravi,2000)		 		